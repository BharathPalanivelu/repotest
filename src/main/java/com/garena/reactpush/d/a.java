package com.garena.reactpush.d;

import com.garena.reactpush.data.BundleState;
import com.garena.reactpush.data.Manifest;

public interface a {
    boolean a();

    boolean a(Manifest manifest, BundleState bundleState);
}
